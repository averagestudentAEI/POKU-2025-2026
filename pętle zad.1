#include<stdio.h>
#include<math.h>
main()
{
	int N,i,j;
	float liczba,suma,W0,aryt,M0;
	scanf("%d",&N);
	suma=0;
	W0=0;
	aryt=0;
	M0=0;
	j=1;
	for(i=1;i<=N;i=i+1)
	{
	
	scanf("%f",&liczba);
		{
			suma=suma+liczba;
			
			if(liczba>-5)
				W0=W0+liczba;
			
			aryt=aryt+(liczba/N);
			
			if(liczba<10)
			{
				M0=M0+liczba;
				j=j+1;
			}
				
			
			
		}
	}
	M0=M0/j;
	printf("costam:%f",suma);
	printf("\ncostam:%f",W0);
	printf("\ncostam:%f",aryt);
	printf("\ncostam:%f",M0);
}
